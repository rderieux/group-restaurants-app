/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RestaurantsFavoritesComponent } from './restaurants-favorites.component';

describe('Component: RestaurantsFavorites', () => {
  it('should create an instance', () => {
    let component = new RestaurantsFavoritesComponent();
    expect(component).toBeTruthy();
  });
});
